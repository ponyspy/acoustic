extends layout

block header_meta
	- var content_title= 'acoustic treatment | live'
	- var menu_active = 'live'

block header_include
	- var include_css = ['live.css']
	- var include_js = ['live.js']

block content
	.panel_block.left: .panel_inner
		each event in events
			.live_item
				.item_num= event.numb
				.item_title
					span.title_main= event.title
					//- span= ' '
					//- span.title_alt= 'alt title'

	.panel_block.right: .panel_inner right