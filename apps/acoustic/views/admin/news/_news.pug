.list_items
	each n_item in news
		.list_item
			a.item_rm.n_item(href='#', id=n_item._id) -- удалить
			a.item_title(href='/admin/news/edit/' + n_item._id, class=n_item.status =='hidden' ? 'hidden': '')= n_item.title ? n_item.title : '!!! ПУСТОЕ ПОЛЕ !!!'
			span.item_preview
				span= ' --> '
				a(href='/news/' + (n_item.sym ? n_item.sym : n_item._short_id), target='_blank') превью

.list_counter
	a.list_back(href='#'): b назад
	span= ' - '
	select.list_select
		- var i = 0;
		while i < count
			option(value=i selected= (i == skip / 10) )= i+1
			- i++
	span= ' - '
	a.list_next(href='#'): b вперед